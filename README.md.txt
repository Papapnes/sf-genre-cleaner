# Salesforce Genre Cleaner

Streamlit app to generate Nom_complet and detect Gender (Male/Female only) from Salesforce exports.

## Run locally
pip install -r requirements.txt
streamlit run app.py
